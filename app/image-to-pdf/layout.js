export const metadata = {
  title: "Image to PDF Converter | SnapClip",
  description: "Convert multiple images into a single PDF document instantly right in your browser.",
};

export default function Layout({ children }) {
  return <>{children}</>;
}
