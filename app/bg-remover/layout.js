export const metadata = {
  title: "AI Background Remover | SnapClip",
  description: "Instantly remove backgrounds from images using AI directly in your browser. 100% free and fast.",
};

export default function Layout({ children }) {
  return <>{children}</>;
}
